openapi: 3.1.0
info:
  title: Lucia's Proton and Hedera Integration
  description: |
    This API integrates Proton services (Mail, Drive, VPN, Pass) with Hedera for decentralized identity and secure transactions. GPT actions can manage emails, files, passwords, VPN, and secure blockchain-based tasks like payments and identity verification.
  version: 1.0.0
servers:
  - url: https://api.luci-digital.com/v1
    description: Main Production Server

 /content/moderation:
    post:
      operationId: moderateContent
      summary: Moderate user-generated content
      description: |
        Ensures user-generated content adheres to community guidelines by integrating AI moderation tools.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                contentId:
                  type: string
                  description: ID of the content to be moderated
                contentText:
                  type: string
                  description: The text of the content to be moderated
      responses:
        '200':
          description: Content moderated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  isApproved:
                    type: boolean
                    description: Whether the content passed moderation
                  reasons:
                    type: array
                    items:
                      type: string
                    description: Reasons for rejection if not approved

  /feedback/submit:
    post:
      operationId: submitFeedback
      summary: Submit user feedback on Lucia interactions
      description: |
        Allows users to provide feedback on their interactions with Lucia, which can be used to improve AI responses and overall user experience.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                userId:
                  type: string
                  description: The user's ID
                feedbackText:
                  type: string
                  description: The feedback provided by the user
                interactionId:
                  type: string
                  description: ID of the interaction the feedback is about
      responses:
        '200':
          description: Feedback submitted successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    description: Whether the feedback submission was successful

  /knowledge-base/query:
    post:
      operationId: queryKnowledgeBase
      summary: Query the knowledge base for information
      description: |
        Allows Lucia to query a knowledge base to provide accurate information to users.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                queryText:
                  type: string
                  description: The query or question to search in the knowledge base
      responses:
        '200':
          description: Knowledge base query successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        title:
                          type: string
                          description: Title of the knowledge base article
                        snippet:
                          type: string
                          description: A short excerpt of the article
                        url:
                          type: string
                          description: URL to the full article